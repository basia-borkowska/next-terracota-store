{
  "name": "next-terracota-store-monorepo",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "scripts": {
    "build": "npm --workspace packages/types run build && npm --workspace apps/api run build && npm --workspace apps/web run build",
    "dev": "concurrently -n web,api -c cyan,magenta \"npm --workspace apps/web run dev\" \"npm --workspace apps/api run dev\""
  }
}
